{
  "version": 2,
  "buildCommand": "pnpm install && pnpm --filter @workspace/db build && pnpm --filter @workspace/api-zod build && pnpm --filter @workspace/integrations-openai-ai-server build && pnpm --filter @workspace/api-client-react build && pnpm --filter @workspace/cbse-tutor build",
  "outputDirectory": "artifacts/cbse-tutor/dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "NODE_ENV": "production"
  }
}
